# .kodiak.toml
# Minimal config. version is the only required field.
version = 1
approve.auto_approve_usernames = [ "joeldenning", "renovate" ]
merge.method = "squash"
merge.delete_branch_on_merge = true
merge.require_automerge_label = false